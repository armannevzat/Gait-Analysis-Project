# Decision log

One line per decision: date, what, why. This becomes the methods section.

| Date | Decision | Reason |
|---|---|---|
| 2026-09-16 | Raw and processed data excluded from the repo | GPS traces reveal personal locations |
| 2026-09-18 | Derive step length from speed and cadence rather than using Garmin's `step_length` field | Running-dynamics fields are not recorded on all devices/years; derived value agreed with Garmin's to within 0.6% on the test activity (1.068 m vs 1.074 m) |
| 2026-09-18 | Treat Garmin `step_length` as a cross-check only, not validation | Garmin almost certainly derives it from speed and cadence too, so agreement confirms the arithmetic, not physical accuracy |
| 2026-09-18 | Step frequency = (cadence + fractional_cadence) x 2 | Garmin records cadence for one leg, with the decimal part stored separately |
| 2026-09-18 | Use `enhanced_speed` / `enhanced_altitude`, falling back to `speed` / `altitude` | Newer devices record only the enhanced fields; older files may use the original names |
| 2026-09-18 | Candidate rule: drop samples with cadence = 0 | Not running. Test activity showed no auto-pause, so the watch keeps recording while stopped at crossings |
| 2026-09-18 | Candidate rule: drop samples with step length outside 0.5-2.5 m | Stationary shuffling at crossings produced ~0.48 m at ~214 steps/min; two such clusters in the test activity |
| 2026-09-18 | Exclude GPX activities from the analysis | Verified: no cadence field in three sampled GPX files (checked full file text, not just the header). Cost is larger than the 3% estimate - it removes all 12 runs of 2022 and 19 of 56 runs in 2023 |
| 2026-09-18 | Record the sampling interval (median gap between samples) for every activity as a column in the tidy dataset | Sampling rate varies by device and era; needed as a filter, a covariate, and a QC statistic |
| 2026-09-18 | Split or flag gaps longer than 30 s; measure time-into-run as moving time, not wall-clock time | Oldest test activity contained a single 508 s stop inside a 1,375 s wall-clock duration; an unflagged pause would corrupt any within-run fatigue slope |
| 2026-09-18 | QC report (Chunk 3) to include, by year: median sampling interval, count of gaps > 30 s, total paused time | Turns the smart-recording question below into a decision made on counts rather than on one file |
| 2026-09-18 | Confirmed rule: drop samples with cadence missing or 0 (27,175 rows, 2.1% of the dataset) | Step frequency is underivable without cadence. Median speed in these rows is 0, so most are genuine stops, but the 90th percentile is 2.93 m/s: at least 10% are sensor dropouts at running speed, not standing still. Both are dropped, but the dropout share explains why some windows later fall short of the minimum sample count |
| 2026-09-18 | Running filter: drop samples with per-leg cadence < 67 steps/min | 5% of samples with non-zero cadence were below 1.82 m/s, i.e. walking with steps still being counted. Walking is a different gait with a different step length-frequency relationship: it would bend the Analysis A fit, and walk breaks late in long runs would read as fatigue in Analysis B. Log-scale cadence histogram is clearly bimodal: walking mode peaks at 58, running mode at ~80-84, density trough at 70. Threshold set at 67, just below the trough, to bias borderline samples toward being retained as running rather than discarded. Cadence separates the two gaits more cleanly than speed does. Consequence: Analysis B describes running gait only, not whether or when walking occurs |
| 2026-09-18 | No explicit upper speed cap | The implausible speeds (99th percentile 9.1 m/s, max 14.4 m/s) sit almost entirely in cadence = 0 rows, which are dropped. Among rows with valid cadence the 99.99th percentile is 6.19 m/s (~2:42/km), plausible for strides. Remaining isolated GPS spikes are handled by the median filter and the 0.5-2.5 m step-length bound |
| 2026-09-18 | Drop the first 60 s of every activity (24,108 rows, 1.8%) | GPS and cadence settle over the opening seconds; consequence is that moving time runs ~60 s behind time-since-start, a constant offset that does not affect within-run fatigue slopes |
| 2026-09-18 | Cleaning cascade applied: 1,325,123 -> 1,208,740 samples (116,383 dropped, 8.8%); all 408 activities survive | Component counts, noting the masks overlap: cadence missing or 0 = 27,175 (25,129 exactly zero, 2,046 NaN); walking, cadence 1-66 = 67,960 (5.1%); first 60 s = 24,108; speed missing = 53. The union is smaller than the sum because every cadence = 0 row also satisfies cadence < 67 |
| 2026-09-18 | "Gap" after cleaning means a break in *running*, not a break in *recording* | Post-cleaning: 844 gaps > 30 s across 227 of 408 activities. The Chunk 3 QC report counted 68 activities with a recording pause > 30 s. Both are correct and they measure different things - dropping stopped and walking samples opens a hole in the timeline wherever a crossing or walk break occurred. Windowing uses the post-cleaning definition |
| 2026-09-18 | Moving-time construction validated against the Chunk 3 QC table for all 408 activities | Compared per-activity max `moving_s` against `wall_clock_s - paused_s`. No activity has a longer moving time after cleaning than before (0 of 408), which is the failure mode a broken `clip(upper=nominal)` would produce. Median difference 130 s = the 60 s cut plus ~1 min of walk breaks, matching expectation |
| 2026-09-18 | Activity-level inclusion: require >= 600 s of surviving running time; carry retained fraction as a per-activity covariate rather than filtering on it | Two distinct defects share the short-surviving-time tail: genuinely short but clean runs (e.g. 685 s surviving of 745 s, 92% retained) and walk-contaminated runs (as low as 15% retained). The 600 s rule mirrors the upstream moving-time filter and costs 5 activities / 0.15% of running time. A retention threshold was rejected as a *filter* because its drops are year-correlated - at >= 70% it removes 12 of 131 activities in 2024 and 0 of 149 in 2025 and 0 of 88 in 2026, i.e. it cuts along the exact axis Analysis A compares. That gradient is plausibly real behaviour (less walking as fitness improved from 2023 onward) and so is evidence, not noise. Consistent with the option-3 stance already taken on sampling rate |
| 2026-09-18 | Analyses A and B to be re-run excluding low-retention activities as a sensitivity check | This is the cost of not filtering on retention above. The check is owed, not optional |
| 2026-09-18 | Speed smoothing: 5-sample centred rolling median, applied within activity, to 1 Hz activities only | Justified as spike removal, not smoothing. Effect is small because the cadence filter already removed the worst spikes: post-cleaning max speed is 7.96 m/s (vs 14.4 m/s pre-cleaning), median change is 0.000 m/s, and only 1,159 samples (0.10%) move by more than 0.5 m/s. Retained because ~420 samples move by more than 1 m/s and each would drag a 30 s window mean. Filter width is set in TIME not samples: 5 samples at 1 Hz is ~5 s, whereas at 6 s sampling it would span 30 s, i.e. the entire analysis window |
| 2026-09-18 | Median filter is allowed to cross the 844 post-cleaning gap boundaries | About 2 samples either side of each break mix values from across it, ~3,400 samples. Not special-cased, because the filter's total effect is 0.10% of samples; recorded so the choice is explicit rather than unnoticed |
| 2026-09-18 | Keep the 7 surviving activities sampled at 6 s (5 from 2023, 2 from 2021) rather than dropping them | Quantified rather than assumed. A 30 s window holds 5 samples instead of 30, so its mean carries sqrt(6) ~ 2.4x the standard error. Because speed is a PREDICTOR in Analysis A, that noise causes regression dilution (slope biased toward zero), which would masquerade as a genuine year effect in the 2023 vs 2026 comparison - the exact comparison the analysis exists to make. Measured on this dataset: median within-window speed SD 0.034 m/s, median between-window SD within a run 0.213 m/s. Implied slope attenuation is 0.1% at n=30 and 0.5% at n=5. A 0.4 percentage point difference is negligible against any reportable year effect, and dropping them would cost 2023 five of its 37 activities. Caveat: Garmin smooths speed on-watch, so 0.034 m/s understates true GPS error and the real margin is smaller than these figures imply |
| 2026-09-18 | Carry samples-per-window as a per-window column | Makes the decision above auditable and supports an Analysis A sensitivity check restricted to 1 Hz activities |

## Open question: one anomalous long activity (raised 2026-09-18)

Activity 12671640206: 52.1 km, QC moving time 3:44:39, but only 2,122 s (35 min) of running survives cleaning - an 84% loss, the largest in the dataset. Average pace over the full distance works out at 4:18/km, which is implausibly fast for an activity that is mostly not running by cadence.

Either the recorded distance is wrong, or this is an event with an unusual structure. It passes the 600 s inclusion rule, so it is currently in the analysis. To inspect before Analysis A.

## Open question: irregular sampling in early years (raised 2026-09-18, to resolve in Chunk 3)

The oldest test activity (2020) was recorded with Garmin "smart recording": gaps between samples were mostly 6 s, with a spread of 1-5 s, giving 173 samples across 1,375 s. The 2026 test activity was recorded at 1 Hz (2,886 samples across 2,886 s).

Why it matters: 30-second windows would be built from roughly 5 samples in 2020 and 30 in 2026, so a window mean is not strictly the same quantity in each era. Analysis A compares the speed partition across years, so a device-driven change in sampling is a confound that runs along the same axis as the effect of interest.

Cross-check on the same file: mean sampled speed was 3.38 m/s while distance / wall-clock time gave 2.18 m/s. Removing the 508 s stop gives 3,002 m / 867 s = 3.46 m/s, which reconciles the two. Irregularly sampled means are therefore not time-weighted and need care.

Options, to decide once the QC counts show how many activities are affected:
1. Resample every activity to a common time base before windowing, so each window is formed the same way.
2. Restrict the analysis to activities sampled at or near 1 Hz. Cleaner, but may cost the early years and with them the six-year claim.
3. Keep all activities, carry the sampling interval per activity, and re-run the headline results excluding the sparsely sampled ones as a sensitivity check.

Current preference: option 3, with option 2 as the sensitivity check. Not yet decided.
