# Decision log

One line per decision: date, what, why. This becomes the methods section.

| Date | Decision | Reason |
|---|---|---|
| 2026-09-16 | Raw and processed data excluded from the repo | GPS traces reveal personal locations |
| 2026-09-18 | Derive step length from speed and cadence rather than using Garmin's `step_length` field | Running-dynamics fields are not recorded on all devices/years; derived value agreed with Garmin's to within 0.6% on the test activity (1.068 m vs 1.074 m) |
| 2026-09-18 | Treat Garmin `step_length` as a cross-check only, not validation | Garmin almost certainly derives it from speed and cadence too, so agreement confirms the arithmetic, not physical accuracy |
| 2026-09-18 | Step frequency = (cadence + fractional_cadence) x 2 | Garmin records cadence for one leg, with the decimal part stored separately |
| 2026-09-18 | Use `enhanced_speed` / `enhanced_altitude`, falling back to `speed` / `altitude` | Newer devices record only the enhanced fields; older files may use the original names |
| 2026-09-18 | Candidate rule: drop samples with cadence = 0 | Not running. Test activity showed no auto-pause, so the watch keeps recording while stopped at crossings |
| 2026-09-18 | Candidate rule: drop samples with step length outside 0.5-2.5 m | Stationary shuffling at crossings produced ~0.48 m at ~214 steps/min; two such clusters in the test activity |
| 2026-09-18 | Exclude GPX activities from the analysis | Verified: no cadence field in three sampled GPX files (checked full file text, not just the header). Cost is larger than the 3% estimate - it removes all 12 runs of 2022 and 19 of 56 runs in 2023 |
| 2026-09-18 | Record the sampling interval (median gap between samples) for every activity as a column in the tidy dataset | Sampling rate varies by device and era; needed as a filter, a covariate, and a QC statistic |
| 2026-09-18 | Split or flag gaps longer than 30 s; measure time-into-run as moving time, not wall-clock time | Oldest test activity contained a single 508 s stop inside a 1,375 s wall-clock duration; an unflagged pause would corrupt any within-run fatigue slope |
| 2026-09-18 | QC report (Chunk 3) to include, by year: median sampling interval, count of gaps > 30 s, total paused time | Turns the smart-recording question below into a decision made on counts rather than on one file |
| 2026-09-18 | Confirmed rule: drop samples with cadence missing or 0 (27,175 rows, 2.1% of the dataset) | Step frequency is underivable without cadence. Median speed in these rows is 0, so most are genuine stops, but the 90th percentile is 2.93 m/s: at least 10% are sensor dropouts at running speed, not standing still. Both are dropped, but the dropout share explains why some windows later fall short of the minimum sample count |
| 2026-09-18 | Running filter: drop samples with per-leg cadence < 67 steps/min | 5% of samples with non-zero cadence were below 1.82 m/s, i.e. walking with steps still being counted. Walking is a different gait with a different step length-frequency relationship: it would bend the Analysis A fit, and walk breaks late in long runs would read as fatigue in Analysis B. Log-scale cadence histogram is clearly bimodal: walking mode peaks at 58, running mode at ~80-84, density trough at 70. Threshold set at 67, just below the trough, to bias borderline samples toward being retained as running rather than discarded. Cadence separates the two gaits more cleanly than speed does. Consequence: Analysis B describes running gait only, not whether or when walking occurs |
| 2026-09-18 | No explicit upper speed cap | The implausible speeds (99th percentile 9.1 m/s, max 14.4 m/s) sit almost entirely in cadence = 0 rows, which are dropped. Among rows with valid cadence the 99.99th percentile is 6.19 m/s (~2:42/km), plausible for strides. Remaining isolated GPS spikes are handled by the median filter and the 0.5-2.5 m step-length bound |
| 2026-09-18 | Drop the first 60 s of every activity (24,108 rows, 1.8%) | GPS and cadence settle over the opening seconds; consequence is that moving time runs ~60 s behind time-since-start, a constant offset that does not affect within-run fatigue slopes |
| 2026-09-18 | Cleaning cascade applied: 1,325,123 -> 1,208,740 samples (116,383 dropped, 8.8%); all 408 activities survive | Component counts, noting the masks overlap: cadence missing or 0 = 27,175 (25,129 exactly zero, 2,046 NaN); walking, cadence 1-66 = 67,960 (5.1%); first 60 s = 24,108; speed missing = 53. The union is smaller than the sum because every cadence = 0 row also satisfies cadence < 67 |
| 2026-09-18 | "Gap" after cleaning means a break in *running*, not a break in *recording* | Post-cleaning: 844 gaps > 30 s across 227 of 408 activities. The Chunk 3 QC report counted 68 activities with a recording pause > 30 s. Both are correct and they measure different things - dropping stopped and walking samples opens a hole in the timeline wherever a crossing or walk break occurred. Windowing uses the post-cleaning definition |
| 2026-09-18 | Moving-time construction validated against the Chunk 3 QC table for all 408 activities | Compared per-activity max `moving_s` against `wall_clock_s - paused_s`. No activity has a longer moving time after cleaning than before (0 of 408), which is the failure mode a broken `clip(upper=nominal)` would produce. Median difference 130 s = the 60 s cut plus ~1 min of walk breaks, matching expectation |
| 2026-09-18 | Activity-level inclusion: require >= 600 s of surviving running time; carry retained fraction as a per-activity covariate rather than filtering on it | Two distinct defects share the short-surviving-time tail: genuinely short but clean runs (e.g. 685 s surviving of 745 s, 92% retained) and walk-contaminated runs (as low as 15% retained). The 600 s rule mirrors the upstream moving-time filter and costs 5 activities / 0.15% of running time. A retention threshold was rejected as a *filter* because its drops are year-correlated - at >= 70% it removes 12 of 131 activities in 2024 and 0 of 149 in 2025 and 0 of 88 in 2026, i.e. it cuts along the exact axis Analysis A compares. That gradient is plausibly real behaviour (less walking as fitness improved from 2023 onward) and so is evidence, not noise. Consistent with the option-3 stance already taken on sampling rate |
| 2026-09-18 | Analyses A and B to be re-run excluding low-retention activities as a sensitivity check | This is the cost of not filtering on retention above. The check is owed, not optional |
| 2026-09-18 | Speed smoothing: 5-sample centred rolling median, applied within activity, to 1 Hz activities only | Justified as spike removal, not smoothing. Effect is small because the cadence filter already removed the worst spikes: post-cleaning max speed is 7.96 m/s (vs 14.4 m/s pre-cleaning), median change is 0.000 m/s, and only 1,159 samples (0.10%) move by more than 0.5 m/s. Retained because ~420 samples move by more than 1 m/s and each would drag a 30 s window mean. Filter width is set in TIME not samples: 5 samples at 1 Hz is ~5 s, whereas at 6 s sampling it would span 30 s, i.e. the entire analysis window |
| 2026-09-18 | Median filter is allowed to cross the 844 post-cleaning gap boundaries | About 2 samples either side of each break mix values from across it, ~3,400 samples. Not special-cased, because the filter's total effect is 0.10% of samples; recorded so the choice is explicit rather than unnoticed |
| 2026-09-18 | Keep the 7 surviving activities sampled at 6 s (5 from 2023, 2 from 2021) rather than dropping them | Quantified rather than assumed. A 30 s window holds 5 samples instead of 30, so its mean carries sqrt(6) ~ 2.4x the standard error. Because speed is a PREDICTOR in Analysis A, that noise causes regression dilution (slope biased toward zero), which would masquerade as a genuine year effect in the 2023 vs 2026 comparison - the exact comparison the analysis exists to make. Measured on this dataset: median within-window speed SD 0.034 m/s, median between-window SD within a run 0.213 m/s. Implied slope attenuation is 0.1% at n=30 and 0.5% at n=5. A 0.4 percentage point difference is negligible against any reportable year effect, and dropping them would cost 2023 five of its 37 activities. Caveat: Garmin smooths speed on-watch, so 0.034 m/s understates true GPS error and the real margin is smaller than these figures imply |
| 2026-09-18 | Carry samples-per-window as a per-window column | Makes the decision above auditable and supports an Analysis A sensitivity check restricted to 1 Hz activities |

| 2026-09-21 | Analysis A primary specification: gradient as a COVARIATE, not a filter - `log_L ~ log_v + grad_pct`, cluster-robust SEs by activity | The brief specified restricting to flat windows (|gradient| < 1%). That filter was rejected as primary because window gradient is noisier than the terrain it measures. Estimated from the lag-1 autocorrelation of gradient between consecutive windows within a run (median rho = 0.44): terrain SD 1.80% vs noise SD 2.02%, implying ~1.7 m of altitude error over a median 83 m window - consistent with barometric altimeter error. A 1% threshold therefore sits below the noise floor. Worse, the noise is speed-dependent: a slow window covers less ground so the same altitude error becomes a larger gradient, and the slowest distance quintile passes the flat filter only 34.7% of the time against 47.7% for the fastest. Since Analysis A regresses step length ON speed, that filter truncates the low end of the predictor range. Caveat recorded: real hills are autocorrelated beyond 30 s, so rho = 0.44 attributes some terrain to noise and 2.02% is an upper bound |
| 2026-09-21 | Flat-window subset (|grad| < 1%) retained as the pre-registered robustness check | Pooled beta_L = 0.8785 on 16,085 flat windows against 0.8781 primary. The selection bias identified above is real but moves the estimate by 0.0004. Reporting both is stronger than either alone: a bias was identified, quantified, and shown not to matter |
| 2026-09-21 | Standard errors clustered by activity throughout | 37,315 windows come from 370 runs; windows within a run share shoes, terrain, legs and day. Unclustered SEs would be several-fold too small |
| 2026-09-21 | ANALYSIS A RESULT: beta_L = 0.8781 (SE 0.0062), beta_f = 0.1219, summing to exactly 1 | A 1% increase in speed is carried by a 0.88% longer step and a 0.12% quicker turnover. Worked example from the fit: 2.5 -> 3.5 m/s (+40% speed) gives step length 0.930 -> 1.250 m (+34.4%) and frequency 161.2 -> 168.0 spm (+4.2%). The sum-to-one is an identity (log v = log f + log L with a shared design matrix), so it is a CODE CHECK, not evidence; likewise the R2 of 0.965 on log_L ~ log_v is near-tautological and must not be reported as explanatory power |
| 2026-09-21 | Year-by-year partition: differences are NOT robust; do not report a year effect | Naive interaction test across 2023-2026 gives F = 3.15, p = 0.025. Restricting to the speed range common to all four years (2.53-3.49 m/s) gives F = 1.84, p = 0.139. Mechanism: the log-log relationship is slightly concave (log_v^2 coefficient -0.042, p = 0.068) and the years span different speed ranges - 2023 reached 3.96 m/s at its 95th percentile against 2025's 3.49 - so the naive test partly compared stretches of a curve rather than years. Matched beta_L: 2023 = 0.898, 2024 = 0.886, 2025 = 0.895, 2026 = 0.847. Counter-caution: matching cuts 2023 from 2,408 to 1,388 windows, so some loss of significance is loss of power. The claim is "not robust", not "absent" |
| 2026-09-22 | ANALYSIS B: the within-run fatigue signature is ABSENT in training runs, and the initial positive result was a warm-up effect | Per-run regressions (93 runs with >= 60 min moving time; 85 after the gradient requirement), one slope per run, aggregated by t-test - never pooled, since the run is the unit of evidence. Naive `log_L ~ t` gives +0.780% per 10 min, i.e. step length LENGTHENS. With speed and gradient controlled: +0.098% [+0.055, +0.141]. Splitting each run at 30 minutes resolves it: first 30 min +0.549% [+0.441, +0.657], after 30 min -0.018% [-0.073, +0.036], p = 0.51. The entire effect is warm-up. Corroborated by duration: 60-90 min runs +0.126%, 90-120 min +0.095%, >120 min +0.007% (p = 0.82), consistent with a fixed early-run effect diluted over longer runs. No intensity gradient (HR tertiles: +0.129 / +0.076 / +0.088) |
| 2026-09-22 | Selection by the walking filter was tested as an explanation and REJECTED | Concern was real: non-running time accumulates 2.5x faster in the last third of a long run (0.130 vs 0.052 seconds per second of running), gap-flagged windows nearly triple (0.9% -> 2.4%), and mean step length rises across thirds (1.042 -> 1.087 m) - i.e. fatigue expresses as walk breaks, which cleaning deletes, leaving a biased sample of the best remaining running. But the slope is stable as walk-affected runs are stripped out: all 85 runs +0.098%, pause rate < 0.05 (44 runs) +0.113%, < 0.02 (26 runs) +0.104%, < 0.02 with zero gaps (18 runs) +0.110%. Per-run slope also correlates NEGATIVELY with pause rate (-0.24), the opposite sign to selection inflation |
| 2026-09-22 | Exclude the first 30 minutes from all Analysis B fatigue estimates | Same rationale as the existing 60 s cut: an identified non-fatigue transient that would otherwise be averaged into every reported slope |
| 2026-09-22 | Report the Analysis B null as an EXCLUSION, not a failure to detect | Post-warm-up CI of [-0.073, +0.036]% per 10 min over a 50-minute stretch bounds the total step-length change between -0.37% and +0.18%. The literature's fatigue signature is a 2-5% shortening (Olaya-Cuartero et al. 2024), which this interval firmly excludes. Interpretation: easy aerobic running at ~2.9 m/s does not fatigue a trained athlete the way exhaustive laboratory protocols do. This makes the 70.3 run split (figure 5) the genuine fatigue test rather than a closing illustration |

| 2026-09-22 | Device and sensor identity extracted from every FIT file (`load_device()` in loader.py, cached to `data/processed/device_info.parquet`) | Cadence is a sensor output, so a hardware change is a rival explanation for any longitudinal cadence claim. Nothing in the tidy dataset recorded the device until now. Two changes found, both material |
| 2026-09-22 | Watch changed during 2023: Forerunner 235 -> Forerunner 255 | By year: 2021 fr235 (2 activities); 2023 fr235 (5) and fr255 (31); 2024-2026 fr255 only. The switch predates the 2024-2026 cadence rise, so it does not explain figure 6 |
| 2026-09-22 | HRM-Pro Plus chest strap (reporting `stride_speed_distance`) used on 53% of 2023 runs and 73% of 2024 runs, then 0% in 2025 and 4.5% in 2026 | The sensor change coincides almost exactly with the period of the apparent cadence rise. This is the confound that nearly invalidated figure 6 |
| 2026-09-22 | Strap-vs-wrist bias measured within year on 2023-2024, where both sensors are present: strap reads 0.77% LOW (SE 0.26, p = 0.003), i.e. 1.27 steps/min below the wrist at 163.7 spm | Estimating the bias within year holds training adaptation constant and isolates the sensor. Both years must be used because 2025 has no strap runs at all |
| 2026-09-22 | FIGURE 6 CORRECTED: cadence at 3.0 m/s estimated from WRIST-ONLY runs; chest-strap runs shown but excluded from the estimate | Wrist-only: 2023 = 163.89 [162.97, 164.82] (16 runs), 2024 = 164.54 [163.67, 165.42] (34), 2025 = 164.69 [164.24, 165.13] (144), 2026 = 167.42 [166.91, 167.93] (74). The rise from 2024 to 2026 is +2.88 spm, NOT the +4.25 the uncorrected series gave - roughly a third of the original headline was the sensor swap. Intervals still do not overlap, so the trend survives. Shape also changes: wrist-only 2023-2025 is nearly flat and the entire move occurs in 2026, the Ironman training year |
| 2026-09-22 | Race cadence finding SURVIVES the sensor check | The race used the strap; the strap reads 1.27 spm LOW. Wrist-equivalent race cadence would therefore be ~174.4 spm, not lower. The race-vs-training gap of +6.6 spm is opposite in sign to the sensor bias and about five times its magnitude, so the sensor cannot account for it. The 2026 strap subgroup in the raw table (169.9 spm) is four activities dominated by the race itself and must not be read as a sensor effect |
| 2026-09-22 | RACE RESULT: at matched speed the race gait differs from training gait by +3.9% cadence / -3.9% step length, present from the first kilometre as a LEVEL shift, not a drift | Race 173.1 spm and 1.129 m at 3.26 m/s; training in the 3.15-3.40 m/s band (n = 3,446 windows) 166.5 spm and 1.174 m. Candidate explanation is running off the bike on pre-fatigued cycling legs, but there is one race, no brick runs identified in the dataset, and race shoes and arousal are uncontrolled. Report as a hypothesis with a mechanism, not a demonstrated cause. Note honestly that the partition DID shift for this run - but it had already shifted before the run started, which is not fatigue |
| 2026-09-22 | RACE FATIGUE: no residual shift. Naive slope after 30 min is -0.569%/10min (the literature signature appears in raw data), but controlled for speed and gradient it is +0.094% | Step length fell in the final quarter exactly as much as the pace drop predicts and no more. This is the brief's central question answered in the one genuinely fatigued case: the partition did not move. The race sits at the 73rd percentile of the 85 training-run slopes. Residual-vs-distance slope after the first 5 km is +0.055 %/km (+0.83% over 15 km) - small, and the wrong sign for fatigue |
| 2026-09-22 | Race prediction is corrected to the run's own mean before examining drift | The pooled model carries the whole dataset's baseline; this race sits 3.9% off it from the first kilometre, and uncorrected that constant offset dominates the figure and hides the trend. Mean-correction per run is the Zandbergen et al. (2023) precedent. It separates HOW HE RAN THIS RACE from HOW HIS RUNNING CHANGED DURING IT; only the second is fatigue. The offset is reported as its own result |
| 2026-09-22 | Figure 6 as originally briefed ("did the fatigue slope flatten as volume built?") is not answerable and was replaced | Post-warm-up fatigue slope by year is null in every year: 2024 +0.024 [-0.090, +0.137], 2025 -0.051 [-0.139, +0.037], 2026 +0.032 [-0.038, +0.102]. There is no effect to watch shrink. Replaced with the cadence-level trend, which is a real measurable change over the same period |
| 2026-09-22 | `load_device()` re-reads every frame of every FIT file and takes ~8 minutes across 402 files | Recorded as a known inefficiency: it pays the full Chunk 3 parse cost to read two header fields. Result is cached to parquet so it runs once. An early-exit version risks missing a sensor that pairs mid-run, so the cost is accepted rather than optimised |

| 2026-09-22 | SENSITIVITY CHECKS DISCHARGED - both owed checks pass; no conclusion depends on the contested inclusion decisions | Analysis A beta_L: primary 0.8781 (SE 0.0062, 370 runs); retained >= 0.70 0.8789 (359); retained >= 0.80 0.8796 (343); 1 Hz only 0.8777 (363); wrist-only 0.8698 (270); flat |grad| < 1% 0.8784 (370). Every estimate lies between 0.870 and 0.880; the largest deviation (wrist-only) is ~1.2 SE. Analysis B post-warm-up slope: primary -0.018% [-0.073,+0.036] p=0.51; retained >= 0.70 -0.020% p=0.49; retained >= 0.80 -0.021% p=0.48; wrist-only -0.038% p=0.20. Null under every restriction. The 2026-09-18 decisions to keep low-retention activities and the seven 6 s runs are therefore confirmed as costless |
| 2026-09-22 | Note for the methods: "1 Hz only" and "fr255 watch only" are the SAME subset, not two independent checks | Both return beta_L = 0.8777 on 36,931 windows / 363 runs, because the seven 6 s activities are exactly the old fr235 runs - smart recording was that watch's behaviour. Do not present them as separate robustness checks |
| 2026-09-22 | Per-year Analysis A is unchanged by the retention restriction | 2023 0.8752 -> 0.8718 (28 -> 26 runs); 2024 0.8935 -> 0.8971 (118 -> 109); 2025 and 2026 identical, since neither year contains a run below 70% retention - consistent with the year-correlated retention gradient recorded on 2026-09-18 |

## RESOLVED: the anomalous long activity was a triathlon (raised 2026-09-18, closed 2026-09-22)

Activity 12671640206 (14 July 2024, 52.1 km, 3.74 h) is an Olympic-distance triathlon recorded as a single activity. Reading it by half-hour: 0-30 min, 1.2 km at 0.7 m/s with cadence ~25 (swim); 30 min-2.5 h, 1.2 to 42 km at 4.1-6.7 m/s with no cadence recorded (bike); 2.5-3.7 h, 42 to 52 km at 2.7 falling to 1.9 m/s with cadence 83 falling to 58 (run, fading to walking).

The 84% of moving time the cleaning discarded was the swim and bike legs, discarded correctly - the bike has no cadence, so those samples fail the cadence filter. The 69 surviving windows are genuine running and stay in.

Screened the whole dataset for others: fraction of samples with speed > 5.5 m/s AND no cadence. 5.5 m/s (19.8 km/h) exceeds any sustained running pace in the data, and a bike leg records no cadence; either condition alone false-positives, the conjunction is specific. At a 2% threshold this returns exactly one activity out of 408, with no other activity above 0.02%. The other activities over 30 km (30.1, 32.0, 36.2, 42.6, 45.6 km) all score 0.000 and are genuine long runs.

| 2026-09-22 | Activity 12671640206 identified as an Olympic-distance triathlon, not a 52 km run; its 69 running windows retained, activity flagged `multisport` | Full reasoning in the resolved-question section below. Screen added to the pipeline as a QC check rather than handled as a one-off |
| 2026-09-22 | Training-volume totals must exclude the swim and bike distance of activity 12671640206 | Its 52.1 km is roughly 1.2 km swim, 41 km bike, 10 km run. Figure 6's lower panel counts all 52 km as running, overstating 2024 by ~42 km (1,193 -> ~1,151 km) |
| 2026-09-22 | RACE FINDING REPLICATED: running off the bike shows ~+3% cadence at matched speed, in two independent events two years apart | Olympic tri run leg (Jul 2024, chest strap) = 166.54 spm against strap-matched 2024 training at 161.55 spm over the same 2.75-3.37 m/s band: +3.09% (89 comparison runs). 70.3 Belgrade (Sep 2026, chest strap) = 173.20 spm against 2026 training at 167.62 spm over 3.11-3.38 m/s: +3.33%; 2026 has only two other strap runs so it cannot be strap-matched, and since the strap reads 0.77% low the wrist-equivalent gap is nearer +4%. The 2026-09-22 entry recording this as a single-observation hypothesis is superseded: it now has a replication |
| 2026-09-22 | The off-the-bike interpretation remains a hypothesis, not a demonstrated cause | Both observations are races, so arousal, race shoes and pacing strategy are confounded with running off the bike. There are no brick training sessions in the dataset to break the tie. n = 2 events |

## Open question: irregular sampling in early years (raised 2026-09-18, to resolve in Chunk 3)

The oldest test activity (2020) was recorded with Garmin "smart recording": gaps between samples were mostly 6 s, with a spread of 1-5 s, giving 173 samples across 1,375 s. The 2026 test activity was recorded at 1 Hz (2,886 samples across 2,886 s).

Why it matters: 30-second windows would be built from roughly 5 samples in 2020 and 30 in 2026, so a window mean is not strictly the same quantity in each era. Analysis A compares the speed partition across years, so a device-driven change in sampling is a confound that runs along the same axis as the effect of interest.

Cross-check on the same file: mean sampled speed was 3.38 m/s while distance / wall-clock time gave 2.18 m/s. Removing the 508 s stop gives 3,002 m / 867 s = 3.46 m/s, which reconciles the two. Irregularly sampled means are therefore not time-weighted and need care.

Options, to decide once the QC counts show how many activities are affected:
1. Resample every activity to a common time base before windowing, so each window is formed the same way.
2. Restrict the analysis to activities sampled at or near 1 Hz. Cleaner, but may cost the early years and with them the six-year claim.
3. Keep all activities, carry the sampling interval per activity, and re-run the headline results excluding the sparsely sampled ones as a sensitivity check.

Current preference: option 3, with option 2 as the sensitivity check. Not yet decided.
